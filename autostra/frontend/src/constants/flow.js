/**
 * 文件注释：autostra/frontend/src/constants/flow.js
 *
 * 职责：封装当前模块的初始化、常量、工具函数或组件逻辑。
 * 边界：仅承担本模块职责，不在此处定义跨模块业务规则。
 */

export const DEFAULT_NODES = [
    {
        id: '1',
        type: 'input',
        data: { label: 'Start' },
        position: { x: 250, y: 50 }
    }
];

export const NODE_TYPES = {};